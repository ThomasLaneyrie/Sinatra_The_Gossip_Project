<html>
  <head>
    <title>The Gossip Project</title>
  </head>
  <body>
    <h1>Liste des Gossips</h1>
    <% gossips.each do |gossip| %>
      <ul>
        <li>
          <%= gossip.author %> a dit : "<%= gossip.content %>"
        </li>
      </ul>
    <% end %>

    <h1>Liste des Gossips avec lien</h1>
    <% gossips.each_with_index do |gossip, index| %>
      <p>
        Potin n°<%= index + 1 %>
        <ul>
          <li>Auteur : <%= gossip.author %></li>
          <li>Contenu : <%= gossip.content %></li>
          <li><a href = "/gossips/<%=index+1%>">Commentaires sur le potin</a></li>
          <li><a href = "/gossips/<%=index+1%>/edit/">Modifier le potin</a></li><br/>
        </ul>
      </p>
    <% end %>

    <h1>Toi aussi tu as pleins de potins à raconter à propos des moussaillons ?</h1>
      <h2>> <a href="/gossips/new/">Ajouter un nouveau potin</a></h2>
  </body>
</html>